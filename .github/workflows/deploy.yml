name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: [ self-hosted ]
    steps:
      - name: Deploy to Portainer
        uses: BramKelchtermans/portainer-deploy-action@beta-0.2
        with:
          portainer_url: ${{ secrets.PORTAINER_URL }}
          portainer_api_key: ${{ secrets.PORTAINER_API_KEY }}
          changed_files_path: ${{ github.workspace }}/mani-staging/mani-staging/docker-compose.yaml

