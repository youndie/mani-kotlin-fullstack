name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: [ macos, self-hosted ]
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'corretto'
          java-version: 20
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Build
        run: ./gradlew :server:publishImageToLocalRegistry -dREGISTRY_HOSTNAME=${{vars.REGISTRY_HOSTNAME}} -dREGISTRY_USERNAME=${{secrets.REGISTRY_USERNAME}} -dREGISTRY_PASSWORD=${{secrets.REGISTRY_PASSWORD}} --no-daemon